language: php

sudo: false

matrix:
  include:
    - php: 7.1
      env: SYMFONY_VERSION=^3.4 COMPOSER_FLAGS="--prefer-lowest"
    - php: 7.2
      env: SYMFONY_VERSION=^3.4
    - php: 7.3
      env: SYMFONY_VERSION=^3.4
    - php: 7.3
      env: SYMFONY_VERSION=^4.0
    - php: 7.3
      env: SYMFONY_VERSION=^5.0
    - php: 7.4
      env: SYMFONY_VERSION=^5.0

env:
  global:
    - SYMFONY_VERSION=""

cache:
  directories:
    - $HOME/.composer/cache

before_install:
  - composer self-update
  - if [ "$SYMFONY_VERSION" != "" ]; then composer require --no-update symfony/process:$SYMFONY_VERSION symfony/dependency-injection:$SYMFONY_VERSION symfony/twig-bundle:$SYMFONY_VERSION; fi

install:
  - composer update --prefer-dist --no-interaction $COMPOSER_FLAGS
  - nvm install node
  - nvm use node
  - npm install mjml
  - npx mjml --version

script:
  - ./vendor/bin/phpunit
